extends layout

block content
	h1= title
	p База даных садовых и полевых растений
	<hr>
	button(onclick="location.href='http://192.168.1.155/defbase'")
		img(src='./images/testdb.jpg', style='vertical-align:middle', width = '10%') 
		|Загрузить тестовую базу
	table(style='width:100%', border='1')
		tbody
			each item in cursor
				tr 
					td(style='width:1%') 
						img.image(src = 'data:image/jpeg;base64, ' + item.img)
					td Наименование: <strong>#{item.name}</strong> <br/> Латинское наименование: <strong>#{item.name_lat}</strong> <br/> 
						a(href = item.url) Википедия

